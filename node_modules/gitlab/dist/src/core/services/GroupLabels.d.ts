import { BaseServiceOptions } from '../infrastructure';
import { ResourceLabels } from '../templates';
declare class GroupLabels extends ResourceLabels {
    constructor(options: BaseServiceOptions);
}
export default GroupLabels;
