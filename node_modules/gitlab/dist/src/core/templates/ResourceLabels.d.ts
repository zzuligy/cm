import { BaseRequestOptions, BaseService, BaseServiceOptions, PaginatedRequestOptions, Sudo } from '../infrastructure';
import { ResourceId, LabelId } from '../services';
declare class ResourceLabels extends BaseService {
    constructor(resourceType: string, options: BaseServiceOptions);
    all(resourceId: ResourceId, options?: PaginatedRequestOptions): Promise<import("../infrastructure").GetResponse>;
    create(resourceId: ResourceId, labelName: string, color: string, options?: BaseRequestOptions): Promise<object>;
    edit(resourceId: ResourceId, labelName: string, options?: BaseRequestOptions): Promise<object>;
    remove(resourceId: ResourceId, labelName: string, options?: Sudo): Promise<object>;
    subscribe(resourceId: ResourceId, labelId: LabelId, options?: Sudo): Promise<object>;
    unsubscribe(resourceId: ResourceId, labelId: LabelId, options?: Sudo): Promise<object>;
}
export default ResourceLabels;
